# Environment
NODE_ENV=production

# Future: Database (when launching full app)
# DATABASE_URL=postgresql://user:pass@host:5432/blokko?sslmode=require

# Future: NextAuth (when launching full app)
# NEXTAUTH_URL=https://blokkobld.com
# NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32
