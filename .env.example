# Environment
NODE_ENV=development

# Database
# Development (SQLite):
DATABASE_URL=file:./dev.db
# Production (PostgreSQL - update schema.prisma provider to "postgresql"):
# DATABASE_URL=postgresql://user:password@localhost:5432/blokko

# NextAuth (Authentication)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32

# Production Example:
# NODE_ENV=production
# NEXTAUTH_URL=https://yourdomain.com
# DATABASE_URL=postgresql://user:pass@production-host:5432/blokko?sslmode=require
